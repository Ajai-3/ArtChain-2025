export enum EventType {
    USER_UPDATED = 'user.update',
    USER_CREATED = 'user.created',
    USER_SUPPORTED = 'user.supported',
    EMAIL_VERIFICATION = 'email.verification',
    PASSWORD_RESET_REQUESTED = 'email.password_reset',
    EMAIL_CHANGE_VERIFICATION = 'email.email_change_verification',
}
